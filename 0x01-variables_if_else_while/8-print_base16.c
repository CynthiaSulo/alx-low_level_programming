#include <stdio.h>
/**
  * main - Print numbers of base 16
  *
  * Return: Always 0 (Sucess)
*/
int main(void)
{
	int a;
	int b;
	for (a = 48; a <= 57; a++)

	{
		putchar(a);
	}
	for (b = 97; b <= 102; b++)
	{
		putchar(b);
	}
	putchar('\n');
	return (0);
}

